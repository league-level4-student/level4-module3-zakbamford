
public class Checkpoint {
	/*
	 * 1. yes
	 * 2. Any exception, it will catch all exceptions including ones thrown by the JRE, rather than just the ones thrown within code.
	 * 3. The first block will always trigger, even if the exception is an ArithmeticException. The code will compile.
	 * 4.
	 * 	1. b
	 * 	2. a
	 * 	3. d
	 * 	4. c
	 */
}
